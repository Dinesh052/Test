# Core runtime (server + UI + OpenEnv API)
openenv-core>=0.2.0,<1.0
fastapi>=0.104.0,<1.0
uvicorn>=0.24.0,<1.0
sse-starlette>=1.6.0
pydantic>=2.5.0,<3.0
requests>=2.31.0
httpx>=0.27.0
numpy>=1.24.0,<3.0

# Emotion scoring + Q-network (optional — falls back to keyword scoring without torch)
sentence-transformers>=2.7.0
